let array = [1, 2, 3, 4, 5, 6];
console.log("array length=" + array.lenth);

let arrayLength array.length;

//for loop 

for (let i = 0; i < arrayLength; i++) {
    let element = array[i];
    console.log("element at index" + i + "is" + element);
}